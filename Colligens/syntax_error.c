// mptris -dcigettext.c

int main (){

	if (
	# ifdef _LIBC
	      domain->conv != (__gconv_t) -1
	# else
	#  ifdef HAVE_ICONV
	      domain->conv != (iconv_t) -1
	#  endif
	# endif
	      )
	    {
	    //code
	    }

}
