int main () {

int test;

#if defined(I18N_MB)
test = (b ->flags & b_Title && fontset = buttonFontSet (b); && font = buttonFont (b););
#endif

#if !defined(I18N_MB)
test = (b ->flags & b_Title && font = buttonFont (b););
#endif

if ((test)){

}
}
